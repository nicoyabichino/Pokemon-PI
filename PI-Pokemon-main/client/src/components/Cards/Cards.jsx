import Card from "../Card/Card";
const Cards = () => {
 return (
    <div>
        <p>Este componente debe renderizar un una card por cada elemento del array que me llega de mi server</p>
    </div>
 )   
};

export default Cards;